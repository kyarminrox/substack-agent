export const TITLE_INPUT = 'textarea[placeholder="Title"]';
// TODO: Fallback to data-testid, aria-label, or localized placeholder text

export const BODY_EDITOR = 'div[contenteditable="true"]';
// TODO: Fallback to data-testid or aria-label if editor markup changes

export const PUBLISH_BUTTON = 'button:has-text("Publish")';
// TODO: Fallback to data-testid, aria-label, or text variations in different locales
